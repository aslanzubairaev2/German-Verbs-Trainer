import React from 'react';

const VerbSelector = ({ selected, onChange }) => {
  // This will be more complex, with filtering and grouping
  return (
    <div>
      <h4>Select Verbs</h4>
      <p>(Verb selection UI will be implemented here)</p>
    </div>
  );
};

export default VerbSelector;
